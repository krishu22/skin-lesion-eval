defaults:
  data: ham10000
  model: swin
  loss: logit_adjusted_ce
  train: stage5_logit_adjusted

run_name: stage5_logit_adjusted_swin
output_dir: outputs/stage5_logit_adjusted

wandb:
  project: ham10000
  tags: [stage5, logit_adjusted_ce]
