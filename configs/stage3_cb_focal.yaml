defaults:
  data: ham10000
  model: swin
  loss: cb_focal
  train: stage1_baseline

run_name: stage3_cb_focal_swin
output_dir: outputs/stage3_cb_focal

wandb:
  project: ham10000
  tags: [stage3, cb_focal]
