seed: 42
deterministic: true
batch_size: 32

optimizer:
  name: adamw
  lr: 5.0e-5
  weight_decay: 0.1

scheduler:
  name: cosine_warm_restarts
  T_0: 10
  T_mult: 2
  warmup_steps: 500

max_epochs: 2
# Short patience for dry run
patience: 2
metric_for_best: balanced_accuracy
