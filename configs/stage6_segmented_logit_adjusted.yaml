defaults:
  data: ham10000_segmented
  model: swin
  loss: logit_adjusted_ce
  train: stage5_logit_adjusted

run_name: stage6_segmented_logit_adjusted_swin
output_dir: outputs/stage6_segmented_logit_adjusted

wandb:
  project: ham10000
  tags: [stage6, segmented, logit_adjusted_ce]
